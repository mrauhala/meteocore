[package]
name = "engine-geojson"
version = "0.4.0"
edition = "2021"

[dependencies]
ds-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rstar = "0.12"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "geojson_engine"
harness = false
