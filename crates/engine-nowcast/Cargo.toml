[package]
name = "engine-nowcast"
version = "0.1.0"
edition = "2021"

[dependencies]
# The algorithm modules (motion/advect/skill) are dependency-free pure
# functions; these dependencies serve only the engine module (#522).
ds-core = { path = "../core" }
ds-poll = { path = "../ds-poll" }
chrono = "0.4"
arc-swap = "1"
tokio = { version = "1", features = ["rt", "sync", "time", "macros"] }
tracing = "0.1"

[dev-dependencies]
# Clutter regression fixture (#620): a captured, operator-labelled frame is
# read back as JSON so the detector can be scored against ground truth.
serde_json = "1"
# Phase-0 skill spike (#520): the example loads fixture composites through
# the public GeoTiffEngine — the same decode path the engine consumes via
# Arc<dyn MapEngine>.
engine-geotiff = { path = "../engine-geotiff" }
