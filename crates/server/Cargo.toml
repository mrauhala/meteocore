[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
ds-core = { path = "../core" }
engine-csv = { path = "../engine-csv" }
api-edr = { path = "../api-edr" }
axum = "0.8"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = "0.3"
