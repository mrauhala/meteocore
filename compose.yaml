services:
  dataserver:
    image: ghcr.io/mrauhala/meteocore:main
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./config.toml:/data/config.toml:ro
      - ./testdata:/data/testdata:ro
    environment:
      - RUST_LOG=info
      # - BASE_URL=https://api.example.com
      # - AWS_ACCESS_KEY_ID=
      # - AWS_SECRET_ACCESS_KEY=
